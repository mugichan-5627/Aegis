yfinance==0.2.38
tavily-python==0.3.3
# openai 1.30.1 + modern httpx (>=0.28, which dropped the `proxies` arg) crashes
# every OpenAI() client with "unexpected keyword argument 'proxies'" — this made
# the deployed tribunal silently fall back. Pin the versions verified locally.
openai==2.35.1
httpx==0.28.1
requests==2.31.0
numpy==2.2.3
python-dotenv==1.0.1
